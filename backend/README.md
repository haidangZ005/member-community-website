# Backend

Xem hướng dẫn cài đặt và danh sách API tại [README gốc](../README.md).

Các migration Sprint 1 nằm trong `src/infrastructure/database/postgres/migrations`. Chạy `npm run migrate:up` sau khi cấu hình `DATABASE_URL`.

